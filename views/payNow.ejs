<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://js.stripe.com/v3/"></script>
    <title>Document</title>
</head>
<body>
    <form id="payment-form">
        <div id="stripe-payment">

        </div>
        <button type="submit">Pay</button>
    </form>
    <script>
        window.addEventListener('load', async() => {
            const {publishableKey} = await fetch("/stripeConfig", {
                method: 'POST',
                body: JSON.stringify({stripeSuperSecretMsg: "yes"})
            }).then(r => r.json());

            const {clientSecret} = await fetch("/payNowConfig", {
                method: 'POST',
                body: '{"stripeSuperSecretMsg": "yes"}'
            }).then(r => r.json());

            console.log(publishableKey)
        });
    </script>
</body>
</html>